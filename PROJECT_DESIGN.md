# ReInnoHub 项目设计文档

> Reinsurance Innovation Hub - 再保险创新中心
>
> 一个用于分享再保险数字化转型和AI应用相关项目的信息共享平台

---

## 项目概述

### 定位
面向同事的内部信息共享网站，用于展示和分享再保险行业的数字化转型项目、AI应用案例及相关创新资源。

### 目标用户
公司内部同事，主要在桌面端浏览。

---

## 设计决策记录

### 视觉风格
| 属性 | 选择 | 说明 |
|------|------|------|
| 整体风格 | 杂志/编辑风格 | 精致排版、大图片、高端阅读体验 |
| 配色方案 | 浅色/白色主题 | 干净明亮，突出图片内容 |
| 品牌标识 | 艺术化标题字体 | 独特的文字样式作为品牌识别 |

### 布局设计
| 属性 | 选择 | 说明 |
|------|------|------|
| 布局形式 | 规整网格 | 固定尺寸卡片，整齐专业 |
| 卡片宽度 | 约 280px | 桌面端 3-4 列布局 |
| 响应式优先级 | 桌面端为主 | 移动端基本可用即可 |

### 卡片规格
| 属性 | 规格 |
|------|------|
| 卡片宽度 | 280px |
| 图片尺寸 | 280px × 180px (固定) |
| 卡片尺寸 | 固定大小 |
| 悬停效果 | 上浮 + 阴影加深 |

### 内容元素
每张卡片包含：
- **封面图片** - 项目的视觉展示 (280×180px)
- **标题** - 项目名称
- **简介** - 简短描述文字

### 功能特性
| 功能 | 状态 |
|------|------|
| 内容展示 | ✅ 核心功能 |
| 搜索筛选 | ❌ 暂不需要 |
| 登录系统 | ❌ 暂不需要 |
| 协作功能 | ❌ 暂不需要 |

---

## 技术方案

### 技术栈
- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式方案**: Tailwind CSS
- **布局**: CSS Grid (固定尺寸网格)

### 项目结构
```
src/
  main.tsx           # React 入口
  App.tsx            # 主页面组件
  index.css          # Tailwind 指令和全局样式
  components/        # UI 组件
    Header.tsx       # 顶部导航，包含艺术化标题
    ProjectCard.tsx  # 项目卡片组件
  data/              # 数据模块
    projects.ts      # 项目数据
  types/             # 类型定义
    index.ts         # TypeScript 类型
```

---

## 视觉参考

### 页面布局草图
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│                    ✦ ReInnoHub ✦                        │
│            Reinsurance Innovation Hub                   │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │  图片    │  │  图片    │  │  图片    │  │  图片   │ │
│  │ 280×180  │  │ 280×180  │  │ 280×180  │  │ 280×180 │ │
│  ├──────────┤  ├──────────┤  ├──────────┤  ├─────────┤ │
│  │ 标题     │  │ 标题     │  │ 标题     │  │ 标题    │ │
│  │ 简介...  │  │ 简介...  │  │ 简介...  │  │ 简介... │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │  图片    │  │  图片    │  │  图片    │  │  图片   │ │
│  │ 280×180  │  │ 280×180  │  │ 280×180  │  │ 280×180 │ │
│  ├──────────┤  ├──────────┤  ├──────────┤  ├─────────┤ │
│  │ 标题     │  │ 标题     │  │ 标题     │  │ 标题    │ │
│  │ 简介...  │  │ 简介...  │  │ 简介...  │  │ 简介... │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 卡片悬停效果
```
正常状态:                    悬停状态:
┌──────────┐                ┌──────────┐  ↑
│  图片    │                │  图片    │  │ 上浮 4-6px
├──────────┤    ───────>    ├──────────┤  │
│ 标题     │     hover      │ 标题     │
│ 简介     │                │ 简介     │
└──────────┘                └──────────┘
  浅阴影                      深阴影 + 扩散
```

---

## 内容主题

网站主要展示以下类型的项目信息：

1. **再保险数字化转型**
   - 核心系统现代化
   - 数据平台建设
   - 流程自动化

2. **AI应用案例**
   - 智能核保
   - 风险评估模型
   - 自然语言处理应用
   - 预测分析

3. **行业创新**
   - InsurTech 新兴公司
   - 开源工具和框架
   - 研究报告和白皮书

---

## 后端设计方案

### 技术选型

| 组件 | 选择 | 说明 |
|------|------|------|
| 后端框架 | Node.js + Express | 与前端技术栈统一，全栈 JavaScript |
| 数据库 | SQLite | 轻量级文件数据库，无需安装，适合小型项目 |
| 截图工具 | Puppeteer | 自动抓取网页截图 |
| 文件上传 | Multer | 处理图片上传 |
| 部署方式 | 单体部署 | 前后端在同一项目中 |

### 数据模型

```typescript
interface Project {
  id: string;           // UUID 主键
  title: string;        // 项目标题（必填）
  description: string;  // 项目描述（必填）
  url: string;          // 项目链接（必填）
  imageUrl: string;     // 封面图片路径
  tags: string[];       // 标签数组
  createdAt: string;    // 创建时间 (ISO 8601)
  updatedAt: string;    // 更新时间 (ISO 8601)
}
```

### API 接口设计

| 方法 | 路径 | 功能 | 请求体/参数 |
|------|------|------|-------------|
| `GET` | `/api/projects` | 获取项目列表 | `?sort=createdAt&order=desc&tag=AI` |
| `GET` | `/api/projects/:id` | 获取单个项目 | - |
| `POST` | `/api/projects` | 创建新项目 | `{ title, description, url, tags, imageUrl }` |
| `PUT` | `/api/projects/:id` | 更新项目 | `{ title?, description?, url?, tags?, imageUrl? }` |
| `DELETE` | `/api/projects/:id` | 删除项目 | - |
| `POST` | `/api/screenshot` | 抓取网页截图 | `{ url }` → 返回 `{ imageUrl }` |
| `POST` | `/api/upload` | 上传图片 | `multipart/form-data` |
| `GET` | `/api/export` | 导出所有数据 | 返回 JSON 文件 |
| `GET` | `/api/tags` | 获取所有标签 | 返回标签列表 |

### 功能特性

| 功能 | 状态 | 说明 |
|------|------|------|
| 项目 CRUD | ✅ | 创建、读取、更新、删除 |
| 自动截图抓取 | ✅ | 使用 Puppeteer 抓取网页截图 |
| 手动图片上传 | ✅ | 支持用户上传自定义封面 |
| 标签分类 | ✅ | 支持多标签 |
| 排序功能 | ✅ | 按时间/标题排序 |
| 数据导出 | ✅ | 导出为 JSON 文件 |
| 访问权限 | 公开 | 无需登录，完全公开访问 |

### 后端项目结构

```
server/
├── index.ts              # Express 应用入口
├── routes/
│   ├── projects.ts       # 项目 CRUD 路由
│   ├── screenshot.ts     # 截图服务路由
│   └── upload.ts         # 文件上传路由
├── services/
│   ├── database.ts       # SQLite 数据库操作
│   └── screenshot.ts     # Puppeteer 截图服务
├── middleware/
│   └── errorHandler.ts   # 错误处理中间件
├── uploads/              # 上传的图片存储目录
└── data.db               # SQLite 数据库文件
```

### 截图服务工作流程

```
用户提交 URL
     │
     ▼
POST /api/screenshot
     │
     ├─→ Puppeteer 打开网页
     │         │
     │         ▼
     │    等待页面加载完成
     │         │
     │         ▼
     │    截取视口截图 (1280×800)
     │         │
     │         ▼
     │    裁剪为 4:3 比例
     │         │
     │         ▼
     │    保存到 uploads/ 目录
     │
     ▼
返回 { imageUrl: "/uploads/xxx.png" }
```

### 前后端集成

开发模式：
- 前端: Vite 开发服务器 (port 5173)
- 后端: Express 服务器 (port 3000)
- Vite 配置代理将 `/api` 请求转发到后端

生产模式：
- Express 同时提供 API 和静态文件服务
- 前端构建产物放在 `dist/` 目录
- Express 配置静态文件中间件

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v0.1 | 2025-01-21 | 初始前端设计方案确定 |
| v0.2 | 2025-01-21 | 添加后端设计方案 |

---

## 开发规划

### Phase 1 - 前端原型 ✅
- [x] 设计方案确定
- [x] 前端静态页面实现
- [x] 杂志风格 UI（瀑布流、标签、悬停效果）
- [x] 示例数据展示

### Phase 2 - 后端开发 (当前)
- [ ] Express 服务器搭建
- [ ] SQLite 数据库集成
- [ ] 项目 CRUD API
- [ ] Puppeteer 截图服务
- [ ] 图片上传功能
- [ ] 前后端对接

### Phase 3 - 功能完善
- [ ] 排序和筛选功能
- [ ] 数据导出功能
- [ ] 错误处理优化
- [ ] 生产环境部署配置
